package io.slifer.automation.reporter;

public class HtmlReporter {
    
    private JsonReport json;
    
    public HtmlReporter(JsonReport json) {
        this.json = json;
    }
    
    public void generateReport() {
        // first, build the suite config
    }
}
